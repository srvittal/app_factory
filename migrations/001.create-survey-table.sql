CREATE TABLE survey_data (
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	Surname TEXT,
    Firstname TEXT,
    Contact_Number INTEGER,
	Survey_Date DATE,
    Age INTEGER,
	Favorite_Food TEXT,
    Eat_Out INTEGER,
    Movies INTEGER,
    TV INTEGER,
    Radio INTEGER,
);
